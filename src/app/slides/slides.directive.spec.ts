import { SlidesDirective } from './slides.directive';

describe('SlidesDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidesDirective();
    expect(directive).toBeTruthy();
  });
});
